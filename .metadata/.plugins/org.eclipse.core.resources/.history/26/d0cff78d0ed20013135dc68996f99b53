import java.io.File;
import java.io.FileNotFoundException;
import java.util.ArrayList;
import java.util.Scanner;

public class Jimmy{
	
	public static void main(String[] args) throws FileNotFoundException{
		// avg num season pt / game with allows
		
		//input 1 - seasons to consider
		//2 - teams
		//stats
		
		Scanner input = new Scanner(new File("dummy.txt"));
		
		//do something with line one
		
		input.nextLine();
		ArrayList<int[]> teams = new ArrayList(Integer.parseInt(input.nextLine()));
		for(int i = 0; i < teams.size(); i++){
			String offence = input.next();
			String defense = input.next();
			int[] team = {Integer.parseInt(teamData),Integer.parseInt(teamData)};
			teams.add(team);
		}
		
		
		input.close();
	}
}